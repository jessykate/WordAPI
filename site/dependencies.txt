# non-python dependencies:
# mongodb
# pymongo

# poster (facilitate HTTP POST requests using the standard multipart/form-data encoding)
http://atlee.ca/software/poster/
django 1.2 or above
python-django-piston
python-nltk
# nltk's stopwords data: 
sudo python -m nltk.downloader -d /usr/share/nltk_data all (for all data set, or without the 'all' to run interactively)
